Refresh the ML model flow using dataset:

{{ dataset_path }}

Target metric threshold:

{{ metric_threshold }}

Make the smallest practical change, run validation, and report BLOCKED: if required data or credentials are missing.
